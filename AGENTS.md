# AGENTS.md

This repository is a small Python app for asset allocation configuration
and anti‑FOMO portfolio monitoring. The guidance below is for agentic coding
tools working in this repo.

## Architecture

The project has two entry points:

1. **`run.py`** — CLI for portfolio monitoring (single check or daily scheduler).
2. **`serve.py`** — HTTP server for the Web‑based asset configuration UI.

`serve.py` generates `config.asset.yaml`, which is the **sole source** of
portfolio holdings (stock names, codes, ratios). `config.yaml` no longer
contains a `portfolio` section; it only holds operational settings (thresholds,
AI analysis, data fetch, notification, scheduler).

## Quick Start Commands

- Install dependencies:
  - `pip install -r requirements.txt`
- Configure assets via the Web UI (generates `config.asset.yaml`):
  - `python serve.py`
  - Then open `http://localhost:8080` in your browser.
- Single run (reads `config.yaml` + `config.asset.yaml`):
  - `python run.py`
- Continuous scheduler (daily run at configured time):
  - `python run.py schedule`

## Build/Lint/Test

There is no formal build system, linter, or unit test suite.

- Build: N/A
- Lint: N/A
- Test: N/A

If you add tests, provide a way to run a single test, e.g.:
- `pytest tests/test_x.py::test_name`

## Runtime Notes

- The CLI entrypoint is `run.py`.
- The main app class is `AntiFOMO` in `src/main.py`.
- Asset configuration is generated by `serve.py` (Web UI) or
  `src/asset_configurator.py` (interactive CLI wizard, legacy).
- Market data is fetched via `akshare` (network required).
- AI analysis is optional; relies on OpenAI‑compatible API.

## Configuration Files

- `config.yaml`: operational config (thresholds, AI, data fetch, notification,
  scheduler). Does **not** contain portfolio holdings. Should avoid secrets.
- `config.asset.yaml`: generated by `serve.py`; contains portfolio holdings,
  asset allocation tree, and web assets. This is the sole source of stock
  names, codes, and ratios used by `run.py`.
- `config.example.yaml`: example config for users (no portfolio section).

Environment variables (preferred for secrets):
- `OPENAI_API_KEY` or `AI_API_KEY`
- `OPENAI_BASE_URL` (for compatible providers)

## Code Style Guidelines

### General

- Language: Python 3.x.
- Use ASCII in source files unless file already uses Unicode.
- Favor small, readable functions over deep abstraction.
- Avoid unnecessary comments; explain only non‑obvious behavior.

### Imports

- Standard library first, then third‑party, then local imports.
- Keep imports at top of file.
- Use explicit imports (no wildcard).

### Formatting

- Indentation: 4 spaces.
- Use double quotes for strings in code (matches current codebase).
- Keep line length reasonable (≈ 100–120 chars).
- Prefer f‑strings for formatting.

### Types

- Use type hints for public functions and dataclasses.
- Prefer `Optional[T]` for nullable values.
- Dataclasses are used for data containers (`@dataclass`).

### Naming Conventions

- Classes: `CamelCase`.
- Functions/variables: `snake_case`.
- Constants: `UPPER_SNAKE_CASE`.
- File names: `snake_case.py`.

### Error Handling

- Prefer graceful failure with clear console messages.
- For external calls (network/API), wrap in try/except and return safe defaults
  (e.g., empty DataFrame, `None`) where appropriate.
- Avoid raising exceptions for user‑supplied config unless the system cannot
  proceed. Use error messages instead.

### IO and Side Effects

- Keep configuration parsing in `src/main.py` and `src/asset_configurator.py`.
- Network access is centralized in `src/data_fetcher.py`.
- Notification output is centralized in `src/notification.py`.
- Avoid writing files outside explicit config outputs.

### AI Analysis

- `src/ai_analyzer.py` is the only module that should call LLM APIs.
- Do not hardcode API keys; use environment variables or config.
- If adding providers, keep a consistent interface and config keys.

### Asset Configuration (Web)

- `serve.py` serves the `web/` frontend and provides the config generation API.
- The generated `config.asset.yaml` is the sole source of holdings for `run.py`.
- Keep the web asset schema simple: name, code, amount, type, region, style.

### Asset Configuration Wizard (Legacy CLI)

- `src/asset_configurator.py` is the legacy interactive CLI wizard.
- Keep the interactive workflow simple and resilient to invalid input.
- New categories should be easy to add in `_default_categories()`.
- Generated config must remain YAML‑serializable and human‑readable.

## Repository Conventions

- No Cursor or Copilot rule files are present at this time.
- Keep `config.yaml` free of secrets and free of portfolio holdings.
- Do not auto‑format or reorder YAML keys unless necessary.

## Suggested Manual Verification

- Configure assets: `python serve.py` → open browser → save assets.
- Run the main app: `python run.py`

## If You Add Tests

- Add a `tests/` folder.
- Document how to run a single test in this file.
- Keep tests deterministic (mock network/LLM).
